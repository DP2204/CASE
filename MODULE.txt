

Read the parent/root "AGENTS.md" first.

Now focus ONLY on the "IVisionWeb" module.

This is a documentation/codebase-mapping task only.

For this task:

- Do not modify application source code.
- Do not build or compile.
- Do not run tests.
- Do not start the application.
- Use static source analysis only.

Create/update:

"IVisionWeb/AGENTS.md"

The purpose is to create a compact knowledge map that future coding agents can use instead of repeatedly scanning this large module.

Analyze the structure of:

- "src/main/java"
- "src/main/webapp"
- relevant configuration/resources
- module dependencies

Document:

1. IVisionWeb's responsibility in the overall project.
2. High-level architecture.
3. "src/main/java" structure and important packages.
4. "src/main/webapp" structure and important frontend areas.
5. Important controllers/services/classes.
6. Important frontend pages, JavaScript and resources.
7. Frontend → backend relationships.
8. Important request/data flows.
9. Important configuration.
10. Dependencies on other modules.
11. Important Island-related code/configuration.
12. Existing Edge-related code/configuration, if present.
13. Important files/classes that a future developer is likely to need.
14. "Where to look next" navigation guidance.

Do NOT document every class or file.

Do NOT copy source code into AGENTS.md.

Prioritize relationships, responsibilities, paths and navigation.

When IVisionWeb depends on another module, reference the module and relevant classes/paths rather than documenting that module in detail.

If something cannot be established from the source, mark it:

"UNKNOWN — requires deeper investigation"

Do not guess.

At the end, report:

- what you analyzed
- what you documented
- what you intentionally did not analyze deeply
- important cross-module dependencies
- recommended next documentation pass

Remember: this is only building the knowledge map. Implementation will happen later.




---------

We are creating persistent codebase knowledge for this large enterprise project.

This is the INITIAL CODEBASE-MAPPING PHASE only.

For THIS TASK:

- Do not modify application source code.
- Do not build or compile the project.
- Do not run tests.
- Do not start the application.
- Do not run deployment or migration commands.
- Use static repository/source analysis only.

These restrictions apply ONLY to this documentation task. Future implementation tasks may modify code and build/test the project when appropriate.

Objective

Create a lightweight hierarchical "AGENTS.md" knowledge base that reduces the amount of repository scanning and token/context consumption required in future coding tasks.

The AGENTS.md files should function as a navigation and architecture map, not as a copy of the source code.

Project Context

This is a large existing enterprise application.

The application previously supported Island Mode.

Island Mode is being decommissioned and the application is being moved to Edge Mode.

The eventual implementation work will be handled separately.

Do NOT design or implement the Island → Edge migration now.

Only understand and document the existing codebase.

First Pass

Start from the repository root.

Create/update:

"AGENTS.md"

Analyze the repository structure at a high level.

Document:

- major modules
- responsibility of each module
- important source directories
- technologies/frameworks
- important configuration locations
- important application entry points
- major module dependencies
- important external integrations
- frontend/backend locations
- where Island-related functionality exists
- where Edge-related functionality exists, if already present

Do not deeply inspect every file.

IVisionWeb

IVisionWeb is the primary module of interest because most of the eventual migration changes are expected there.

Identify its relationship with the other modules, but do not deeply document those modules yet.

The next documentation pass will focus specifically on:

"IVisionWeb"

Documentation Structure

Prefer a hierarchy such as:

"AGENTS.md"

"IVisionWeb/AGENTS.md"

"IVisionWeb/src/main/java/AGENTS.md"

"IVisionWeb/src/main/webapp/AGENTS.md"

Create deeper AGENTS.md files only when the size/complexity of a package or feature justifies it.

Important

Optimize for FUTURE AGENTS.

The documentation should answer:

- Where is this functionality?
- Which module owns it?
- Which package/class should I inspect?
- What calls this component?
- What does this component depend on?
- Where should I look next?

Do NOT document:

- trivial classes
- getters/setters
- generated code
- every individual file
- large source-code snippets
- speculative behavior
- implementation proposals

Prefer concise descriptions plus exact paths/classes.

Cross-Module References

When you discover:

"IVisionWeb → ModuleA → ModuleB"

record the relationship and important paths/classes.

Do not copy ModuleA/ModuleB's implementation details into IVisionWeb's documentation.

Those modules can receive their own AGENTS.md later.

Accuracy

Only document what can be verified from the source.

If something is unclear:

"UNKNOWN — requires deeper investigation"

Do not guess.

Final Output

Create/update the appropriate AGENTS.md file(s) for this first repository-level pass.

At the end, report:

1. Major modules discovered
2. AGENTS.md files created
3. Important IVisionWeb dependencies discovered
4. Areas that require deeper documentation
5. Recommended next pass

Again, this is ONLY the knowledge-generation phase.

Do not modify application source code.
Do not build or compile.
Do not test.




------------------66-66------------------


We are creating persistent codebase knowledge for a very large existing enterprise project.

IMPORTANT: This is a DOCUMENTATION / CODEBASE-MAPPING task only.

Do NOT implement anything.
Do NOT modify application source code.
Do NOT refactor anything.
Do NOT propose fixes.

The purpose of this task is to create an efficient "AGENTS.md" knowledge hierarchy so that future OpenCode sessions can understand and navigate this large repository without repeatedly scanning the entire codebase.

Project Context

The application previously supported Island Mode.

Island Mode is now being decommissioned and the application is being moved to Edge Mode.

The eventual implementation work will be performed separately.

For now, we only need accurate knowledge of the existing codebase.

First-pass objective

Analyze the repository at a HIGH LEVEL.

Do NOT deeply inspect every source file.

Identify:

1. Major modules
2. Responsibility of each module
3. Important source directories
4. Build systems and major technologies
5. Relationships/dependencies between modules
6. Main application entry points
7. Important configuration locations
8. Which modules appear to contain frontend code
9. Which modules appear to contain backend code
10. Important external integrations
11. Where Island-related functionality appears to exist
12. Where Edge-related functionality already appears to exist, if any

AGENTS.md strategy

Create/update the ROOT:

"AGENTS.md"

This file should be a compact navigation map for future coding agents.

It should NOT attempt to document the entire codebase.

The root AGENTS.md should contain:

1. Project Overview

What the project is and its major architectural areas.

2. Repository Structure

Important modules/directories and their purpose.

3. Technology Overview

Important languages/frameworks/build systems.

4. Module Map

For each major module:

- responsibility
- important source locations
- dependencies
- whether it contains frontend/backend/shared functionality

5. Important Relationships

Important module-to-module relationships.

6. Island / Edge Context

Only identify where Island/Edge-related functionality appears.
Do not propose migration changes.

7. Where To Look

Give future agents a quick starting point depending on the type of task.

For example:

Frontend task → IVisionWeb/src/main/webapp
Backend task → IVisionWeb/src/main/java
etc.

8. Documentation Hierarchy

Document where deeper AGENTS.md files should exist.

Example:

IVisionWeb/
AGENTS.md

IVisionWeb/src/main/java/
AGENTS.md

IVisionWeb/src/main/webapp/
AGENTS.md

Only create deeper files if the repository structure justifies them.

Important

Because this repository is very large:

DO NOT read every file.

DO NOT put source code inside AGENTS.md.

DO NOT describe trivial classes.

DO NOT create huge documentation.

The purpose is to reduce future token/context consumption.

The AGENTS.md should tell a future agent:

"Where should I look?"

rather than:

"Here is the entire source code."

Cross-module references

When you discover a relationship such as:

IVisionWeb → ModuleA → ModuleB

document the relationship and relevant paths.

Do not duplicate ModuleA or ModuleB's internal implementation details in the root AGENTS.md.

Those modules can receive their own AGENTS.md later.

Accuracy

Only document facts verified from the repository.

Do not guess.

If something is unclear, mark it as:

UNKNOWN — requires deeper investigation.

Final requirement

Before finishing, report:

1. Major modules discovered
2. What was documented
3. What was intentionally not deeply analyzed
4. Recommended next documentation pass
5. Any important cross-module relationships discovered

Again:

THIS TASK IS ONLY FOR CODEBASE KNOWLEDGE.

DO NOT IMPLEMENT THE ISLAND → EDGE MIGRATION.



______________________________________________



<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.bnpparibas.dpw</groupId>
    <artifactId>dpw-bom</artifactId>
    <version>4.0.0-1</version>
    <packaging>pom</packaging>

    <name>DPW Platform BOM</name>

    <!-- ===================================================== -->
    <!-- Properties -->
    <!-- ===================================================== -->
    <properties>
        <java.version>21</java.version>
        <spring.boot.version>3.5.9</spring.boot.version>
        <spring-cloud.version>2023.0.3</spring-cloud.version>

        <!-- Common libs -->
        <commons-io.version>2.15.1</commons-io.version>
        <commons-validator.version>1.7</commons-validator.version>
        <commons-collections4.version>4.4</commons-collections4.version>

        <!-- Mapping -->
        <mapstruct.version>1.6.3</mapstruct.version>
        <lombok.version>1.18.42</lombok.version>

        <!-- OpenAPI -->
        <springdoc.version>2.5.0</springdoc.version>

        <!-- Database -->
        <oracle.ojdbc.version>23.7.0.25.01</oracle.ojdbc.version>

        <!-- Utilities -->
        <poi.version>5.3.0</poi.version>
        <opencsv.version>5.9</opencsv.version>

        <!-- Testing -->
        <cucumber.version>7.14.0</cucumber.version>
        <equalsverifier.version>3.15.3</equalsverifier.version>

        <!-- Other -->
        <jakarta.servlet.version>6.0.0</jakarta.servlet.version>
    </properties>

    <!-- ===================================================== -->
    <!-- Dependency Management -->
    <!-- ===================================================== -->
    <dependencyManagement>
        <dependencies>

            <!-- Spring Boot BOM -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Cloud BOM -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- ================================================= -->
            <!-- Common Libraries -->
            <!-- ================================================= -->

            <dependency>
                <groupId>org.apache.commons</groupId>
                <artifactId>commons-collections4</artifactId>
                <version>${commons-collections4.version}</version>
            </dependency>

            <dependency>
                <groupId>commons-io</groupId>
                <artifactId>commons-io</artifactId>
                <version>${commons-io.version}</version>
            </dependency>

            <dependency>
                <groupId>commons-validator</groupId>
                <artifactId>commons-validator</artifactId>
                <version>${commons-validator.version}</version>
            </dependency>

            <!-- ================================================= -->
            <!-- Database -->
            <!-- ================================================= -->

            <dependency>
                <groupId>com.oracle.database.jdbc</groupId>
                <artifactId>ojdbc11</artifactId>
                <version>${oracle.ojdbc.version}</version>
            </dependency>

            <!-- ================================================= -->
            <!-- OpenAPI (Spring Boot 3 compatible) -->
            <!-- ================================================= -->

            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- ================================================= -->
            <!-- Mapping -->
            <!-- ================================================= -->

            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${mapstruct.version}</version>
            </dependency>

            <!-- ================================================= -->
            <!-- Utilities -->
            <!-- ================================================= -->

            <dependency>
                <groupId>org.apache.poi</groupId>
                <artifactId>poi</artifactId>
                <version>${poi.version}</version>
            </dependency>

            <dependency>
                <groupId>org.apache.poi</groupId>
                <artifactId>poi-ooxml</artifactId>
                <version>${poi.version}</version>
            </dependency>

            <dependency>
                <groupId>com.opencsv</groupId>
                <artifactId>opencsv</artifactId>
                <version>${opencsv.version}</version>
            </dependency>

            <!-- ================================================= -->
            <!-- Servlet API -->
            <!-- ================================================= -->

            <dependency>
                <groupId>jakarta.servlet</groupId>
                <artifactId>jakarta.servlet-api</artifactId>
                <version>${jakarta.servlet.version}</version>
            </dependency>

            <!-- ================================================= -->
            <!-- Testing -->
            <!-- ================================================= -->

            <dependency>
                <groupId>io.cucumber</groupId>
                <artifactId>cucumber-spring</artifactId>
                <version>${cucumber.version}</version>
                <scope>test</scope>
            </dependency>

            <dependency>
                <groupId>io.cucumber</groupId>
                <artifactId>cucumber-junit-platform-engine</artifactId>
                <version>${cucumber.version}</version>
                <scope>test</scope>
            </dependency>

            <dependency>
                <groupId>nl.jqno.equalsverifier</groupId>
                <artifactId>equalsverifier</artifactId>
                <version>${equalsverifier.version}</version>
                <scope>test</scope>
            </dependency>

        </dependencies>
    </dependencyManagement>

        